<h1>Corsairs#edit</h1>
<p>Find me in app/views/corsairs/new.html.erb</p>

<%=form_for @corsair do |f| %>

    <% if @corsair.errors.any? %>
	    <div id="error_explanation">
	      <h2><%= pluralize(@corsair.errors.count, "error") %> prohibited this corsair from being saved: </h2>
	    <ul>
	    <% @corsair.errors.full_messages.each do |msg| %>
	    <li><%= msg %></li>
	    <% end %>
	    </ul>
	    </div>
  	<% end %>

	<%=f.label :first_name, "First name" %><br>
	<%=f.text_field :first_name%><br><br>

	<%=f.label :age, "Age" %><br>
	<%=f.number_field :age%><br><br>

	<%=f.label :bio, "Biography" %><br>
	<%=f.text_field :bio%><br><br>
	
	<%=f.label :slack_handle, "Slack nickname" %><br>
	<%=f.text_field :slack_handle%><br><br>
	
	<%=f.label :github_handle, "Github nickname" %><br>
	<%=f.text_field :github_handle%><br><br>
	
	<%= f.radio_button(:likeness, "kebab") %>
	<%= f.label(:likeness_kebab, "Kebab") %>

	<%= f.radio_button(:likeness, "hummus") %>
	<%= f.label(:likeness_hummus, "Hummus") %>

	<%=f.submit "Update" %>

<% end %>

<%= link_to "Delete", @corsair, method: :delete, data: {confirm: "Are you sure that you want to delete this corsair?"} %>